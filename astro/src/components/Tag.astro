---
import { getTechColor } from '../lib/tagStyles';

interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span class={`px-3 py-1 rounded-full text-xs font-medium border ${getTechColor(label)}`}>
  {label}
</span>
